# data

Jeu de données pour tests et démos.

- Ce qu’il fait: contient des cas synthétiques pour simuler des demandes.
- Exemple simple:
  - `synthetic/sample_case.json` -> utilisé pour POST `/api/client/credit-requests`.
  - Résultat attendu: le backend traite l’exemple et renvoie une réponse stub.
